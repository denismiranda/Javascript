//A constant creates a variable whose value is fixed (cannot be changed)

const number = 42
console.log(number)

/*
const number = 55
console.log(number)
    A constant cannot have its value changed, because of this line of code will give an error
*/